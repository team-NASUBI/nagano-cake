<h3>注文情報確認</h3>
<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
    </tr>
  </thead>
  <tbody>
    <% @cart_products.each do |cart_product| %>
      <tr>
        <td><%= attachment_image_tag cart_product.product, :product_image, :fill, 100, 100, fallback: "no_image.png", size: '100x100' %><%= cart_product.product.name %></td>
        <td><%= (cart_product.product.price * 1.1).floor.to_s(:delimited) %></td>
        <td><%= cart_product.amount %></td>
        <td><%= (cart_product.product.price * cart_product.amount * 1.1).floor.to_s(:delimited) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<table>
  <tr>
    <th>送料</th>
    <td><%= @order.shipping %></td>
  </tr>
  <tr>
    <th>商品合計</th>
    <td><%= @total_price %></td>
  </tr>
  <tr>
    <th>請求額</th>
    <td><%= @order.shipping + @total_price %></td>
  </tr>
</table>

<table>
  <tr>
    <th>支払方法</th>
    <td><%= @order.payment_method %></td>
  </tr>
  <tr>
    <th>お届け先</th>
    <td>
      <%= @order.postal_code %><br>
      <%= @order.address %><br>
      <%= @order.name %>
    </td>
  </tr>
</table>